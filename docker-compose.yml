version: '3'
services:
  aws_lab_web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    restart: always
  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always
